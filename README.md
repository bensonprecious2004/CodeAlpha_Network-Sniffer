# CodeAlpha_Network-Sniffer
A Python-based network packet analyzer built using the Scapy library to capture live network traffic, decode transport layer protocols (TCP/UDP/ICMP), and inspect source/destination metadata for security monitoring
